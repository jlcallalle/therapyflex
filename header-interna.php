<?php
/**
 * Header interno legado.
 *
 * Se conserva por compatibilidad con plantillas antiguas que llamen
 * get_header('interna'), pero delega al header principal para evitar
 * duplicar assets, metadatos y navegacion.
 */
get_header();
